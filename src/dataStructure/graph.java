package dataStructure;

import java.util.Objects;

public class graph {

/**
 *
 * data structure : Maintain an (unordered) linked list of key value pairs
 *  search : scan though all keys to find a match
 *  insert : Scan through all keys until a match is found; if no match found add to front
 *
 * */



}
